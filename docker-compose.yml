services:
  postgres:
    image: postgres:17-alpine
    container_name: fluss-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: fluss
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
